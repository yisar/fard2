
<template name="text">
  <text class="{{props.class||props.className}}">{{props.nodeValue}}</text>
</template>
<template name="button">
  <button class="{{props.class}}" bindtap="{{props.onClick}}">{{props.nodeValue}}</button>
</template>
<template name="image">
  <image class="{{props.class}}" src="{{props.src}}"></image>
</template>
<template name="navigator">
  <navigator class="{{props.class}}" url="{{props.url}}">{{props.nodeValue}}</navigator>
</template>
<template name="component">
  <template is="{{render.name}}" data="{{...render}}"></template>
</template>
    

<template name="view0">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view1">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view2">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view3">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view4">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view5">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view6">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view7">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view8">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view9">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view10">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view11">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view12">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view13">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view14">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view15">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view16">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view17">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view18">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

<template name="view19">
  <view class="{{props.class}}">
    <block wx:for="{{props.children}}" wx:key="">
      <template is="{{item.name}}" data="{{...item}}"></template>
    </block>
  </view>
</template>

